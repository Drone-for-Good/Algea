var chai = require('chai');
var assert = chai.assert;
var should = chai.should();
var expect = chai.expect;
var game =

describe('The game methods', function () {
  describe('The game parameters object:', function(){

    it('should hold room size, food colors, and amount of food added per update',
      function(){
        //Fake test to test if my testing works.
        expect(true).to.equal(true);
    });
  });
});
